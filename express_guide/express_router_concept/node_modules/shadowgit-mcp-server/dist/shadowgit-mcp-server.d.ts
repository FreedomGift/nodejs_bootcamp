/**
 * ShadowGit MCP Server - Main Entry Point
 * Provides read-only Git access, session management and checkpoint creation for AI assistants
 */
export declare class ShadowGitMCPServer {
    private server;
    private repositoryManager;
    private gitExecutor;
    private sessionClient;
    private gitHandler;
    private listReposHandler;
    private checkpointHandler;
    private sessionHandler;
    constructor();
    private setupHandlers;
    start(): Promise<void>;
    shutdown(signal: string): void;
}
