/**
 * Handler for git_command tool
 */
import { RepositoryManager } from '../core/repository-manager';
import { GitExecutor } from '../core/git-executor';
import type { MCPToolResponse } from '../types';
export declare class GitHandler {
    private repositoryManager;
    private gitExecutor;
    constructor(repositoryManager: RepositoryManager, gitExecutor: GitExecutor);
    /**
     * Validate git command arguments
     */
    private isGitCommandArgs;
    /**
     * Handle git_command tool execution
     */
    handle(args: unknown): Promise<MCPToolResponse>;
}
