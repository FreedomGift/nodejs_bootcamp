/**
 * Handler for session management - start and end sessions
 */
import { RepositoryManager } from '../core/repository-manager';
import { SessionClient } from '../core/session-client';
import type { MCPToolResponse } from '../types';
export declare class SessionHandler {
    private repositoryManager;
    private sessionClient;
    constructor(repositoryManager: RepositoryManager, sessionClient: SessionClient);
    /**
     * Start a new work session
     */
    startSession(args: unknown): Promise<MCPToolResponse>;
    /**
     * End an active session
     */
    endSession(args: unknown): Promise<MCPToolResponse>;
    private isStartSessionArgs;
    private isEndSessionArgs;
}
