/**
 * Handler for list_repos tool
 */
import { RepositoryManager } from '../core/repository-manager';
import type { MCPToolResponse } from '../types';
export declare class ListReposHandler {
    private repositoryManager;
    constructor(repositoryManager: RepositoryManager);
    /**
     * Handle list_repos tool execution
     */
    handle(): Promise<MCPToolResponse>;
}
