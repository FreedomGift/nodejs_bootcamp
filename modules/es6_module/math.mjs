const add = (a, b) => a + b;

const subtract = (a, b) => a - b;

const multiply = (a, b) => a * b;

const divide = (a, b) => a / b;

const version = '1.0.0';
const name = 'math';

export { add, subtract, multiply, divide, version, name };
